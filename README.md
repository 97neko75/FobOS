# FobOS

大哥哥用这么恶心的眼神看着我干什么呀~
你不会是想让这么懒的作者写下README吧~
你们可真是杂鱼呢~

好吧好吧，既然你这么想看，那我就勉为其难地介绍一下这个叫做 **Fob** 的小东西。  
它是个操作系统内核，不过别指望它能干正事，它就是个玩具，用来在 QEMU 里卖萌的。

## 它能干什么？

- 开机后给你一个 `>` 提示符，你可以输入一些命令，比如 `help`、`fortune`、`calc`、`meminfo`。
- 支持 Shift 键和 Caps Lock，所以你可以输入大写字母和 `<>?` 之类的符号。
- 屏幕会自动滚动，光标会一闪一闪的（可爱吧）。
- 还内置了一个假的内存查看器，虽然不准但至少不蓝屏。

## 怎么编译？

哼，你不会连交叉编译工具链都没有吧？  
先装好 **i686-elf-gcc** 和 **QEMU**（不会装？那你是真的杂鱼呢~），  
然后双击 `build.bat` 就能生成 `fob.elf` 了。

运行命令：
```bash
qemu-system-x86_64 -kernel fob.elf

如果报错……那就是你的问题，不是我的。

命令列表
help – 显示这个列表（其实你看这README就行了）

hello – 打个招呼

cls / clear – 清屏

reboot – 重启（假的，就是重置虚拟机）

echo – 你说什么它就说什么

about – 关于这个破系统

ver – 版本号

calc – 1+1 之类的小儿科

fortune – 随机鸡汤

meminfo – 内存信息（数字可能很奇怪，别在意）

代码结构
你要是真想看代码，自己翻 boot/ 和 kernel/ 目录吧。
别问为什么这么乱，问就是懒。

许可证
MIT 许可证，随便用，但别找我负责。
