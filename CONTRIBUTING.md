# 贡献指南（写给想帮忙的“杂鱼”们）

大哥哥/大姐姐，既然你点进来了，说明你对这个破烂内核有点兴趣？  
行吧，我把规则说清楚，免得你乱搞把代码炸了还赖我。

## 1. 先保证你能编译

别跟我抱怨“怎么编译不了”，先检查：

- 装了 **i686-elf-gcc** 和 **QEMU** 吗？  
- 运行 `build.bat` 能否生成 `fob.elf`？  
- 能跑 `qemu-system-x86_64 -kernel fob.elf` 看到 `>` 提示符吗？

如果不行，先解决自己的环境问题（网上教程一堆），别来问这种杂鱼问题。

## 2. 你想改什么？

当前急需帮忙的地方（`good first issue` 标签）：

- [ ] **修复退格键重复删除**（`keyboard.c` 里 `\b` 处理写烂了）
- [ ] **屏幕驱动清理**（把 `cursor_row/col` 藏起来，别到处乱用）
- [ ] **增加 `print_paged` 函数**（让长输出自动分页，按空格继续）
- [ ] **支持左右方向键移动光标**（梦一个行内编辑）
- [ ] **给 README 加上真正的截图**（这破内核长啥样别人都不知道）

你挑一个能做的不吹牛的去做。

## 3. 代码风格（虽然本来就很乱，但稍微注意一下）

- **缩进**：用 4 个空格，别用 Tab（混用了我会在 PR 里骂你）。
- **命名**：函数名用小写加下划线，如 `clear_screen`；常量用全大写。
- **注释**：没有硬性要求，但你写得少了别人看不懂，写太多了又啰嗦。反正别写废话。
- **头文件**：需要什么就 `#include` 什么，别瞎包含。

## 4. 提 PR 的流程

1. **Fork** 本仓库到你自己的 GitHub。
2. **创建一个分支**，名字用 `fix/xxx` 或 `feature/xxx`，别在主分支上乱改。
3. **改完后自己先编译测试**，确保至少 `help`、`fortune` 等命令还能用。
4. **提交 PR** 到本仓库的 `main` 分支。  
   - 标题简明扼要，例如 `修复退格键重复删除问题`。  
   - 内容里说清楚你改了哪里、怎么改的、测试结果如何（附上截图更佳）。
5. 等着我（或未来其他维护者）审核。如果过了就合，没过我会告诉你原因（可能骂你杂鱼）。

## 5. 一个忠告

**别试图把 FobOS 改成一个真正的操作系统** —— 它就是个教学玩具。你想加文件系统、图形界面、网络协议栈？出门左转 `CoolPotOS`。  
你要是真想贡献，就帮我把现有的这点破代码整理干净，让后来的人看得懂就行。

## 6. 关于版权

你提交的代码默认采用 **MIT 许可证**，和本仓库一样。别抄别人的代码不注明来源，否则我会把你挂到 `Hall of Shame`。

---

好了，该说的都说了。  
现在，滚去写代码吧，杂鱼。
